// Clasesita Lexema.js
class Lexema {
    constructor(type, value) {
        this.type = type; // Tipo de lexema.
        this.value = value; // Valor del lexema.
    }
}

module.exports = Lexema; // Exportar la clase Lexema.